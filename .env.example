# Required for LLM-assisted scenario-to-map generation.
# Never expose this value in browser code or commit real keys.
ANTHROPIC_API_KEY=

# Optional local API port. Vite proxies /api to this server in development.
FLOWENT_API_PORT=8787
